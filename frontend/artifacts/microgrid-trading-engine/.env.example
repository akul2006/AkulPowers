# Optional remote/local REST origin. The generated client already includes /api
# in every route, so both http://localhost:8080 and http://localhost:8080/api
# are accepted here.
VITE_API_BASE_URL=http://localhost:8080/api