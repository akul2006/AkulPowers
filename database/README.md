# PostgreSQL Database

This folder is reserved for PostgreSQL scripts.

Planned tables:
- nodes
- meter_readings
- energy_trades
- audit_logs
- grid_metrics (if stored rather than calculated)

SQL schema and sample data will be added when the JDBC backend is built.
